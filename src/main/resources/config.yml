# ArkOps-Ai 配置文件

# 语言设置 (en, zh)
language: "zh"

# DeepSeek API 配置
deepseek:
  # DeepSeek API Key
  api-key: "YOUR_API_KEY_HERE"
  # 使用的模型
  model: "deepseek-chat"
  # API 超时时间 (秒)
  timeout: 60
  # 最大响应 token 数
  max-tokens: 2000
  # 温度参数 (创造性, 0-2)
  temperature: 1.0
