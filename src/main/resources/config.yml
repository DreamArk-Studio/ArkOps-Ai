# ArkOps-Ai 配置文件

# OpenAI API 配置
openai:
  # OpenAI API 密钥
  api-key: "YOUR_API_KEY_HERE"
  # 使用的模型
  model: "gpt-5.4"
  # API 超时时间 (秒)
  timeout: 60
  # 最大响应令牌数
  max-tokens: 2000
  # 温度 (创造性，0-1)
  temperature: 0.7
